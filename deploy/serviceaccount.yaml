apiVersion: v1
kind: ServiceAccount
metadata:
  name: detector
  namespace: sentinel
  annotations:
    # Workload Identity: pod auth to GCP uses detector-sa, no key file needed
    iam.gke.io/gcp-service-account: detector-sa@cost-sentinel-13402.iam.gserviceaccount.com
